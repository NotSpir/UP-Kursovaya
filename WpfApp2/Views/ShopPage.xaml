<Page x:Class="WpfApp2.Views.ShopPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp2.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ShopPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel HorizontalAlignment="Left">
                <TextBlock Text="Введите название для поиска:" Width="175" TextAlignment="Right"></TextBlock>
                <TextBox Width="225" Name="TBoxSearch" TextChanged="TBoxSearch_TextChanged"></TextBox>
                <TextBlock Text="Выберите тип:" Width="175" TextAlignment="Right"></TextBlock>
                <ComboBox Width="225" Name="ComboType" DisplayMemberPath="InstrumentTypeName" SelectionChanged="ComboType_SelectionChanged"></ComboBox>
                
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Добавить" Name="BtnAdd" Click="BtnAdd_Click" Width="100px" Background="DeepSkyBlue" Visibility="Hidden"></Button>
            <Button Content="Редактировать" Name="BtnEdit" Click="BtnEdit_Click" Width="100px" Background="DeepSkyBlue" Visibility="Hidden"></Button>
            <Button Content="Удалить" Name="BtnDelete" Click="BtnDelete_Click" Width="100px" Background="DeepSkyBlue" Visibility="Hidden"></Button>
        </StackPanel>
        <ListView Grid.Row="1" Name="LViewTasks" ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Center">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"></WrapPanel>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="20" Width="auto">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"></RowDefinition>
                            <RowDefinition Height="auto"></RowDefinition>
                            <RowDefinition Height="auto"></RowDefinition>
                            <RowDefinition Height="auto"></RowDefinition>
                            <RowDefinition Height="auto"></RowDefinition>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Text="{Binding TaskName}"  TextAlignment="Justify" Width="auto" TextWrapping="Wrap"
                                   HorizontalAlignment="Left" Margin="0 5" FontSize="26" Grid.Row="0"></TextBlock>
                        <TextBlock Text="{Binding CompletionTime, StringFormat={}Время выполнения: {0} минут}"  Grid.Row="1" Margin="0 5 5 5" HorizontalAlignment="Left" FontSize="26" FontWeight="SemiBold"></TextBlock>
                        <TextBlock Text="{Binding Discipline.DisciplineName}"  Grid.Row="2" FontSize="14" HorizontalAlignment="Left" FontWeight="ExtraBold"></TextBlock>
                        <TextBlock Text="{Binding Description}"  Grid.Row="3" FontSize="14" HorizontalAlignment="Left" TextWrapping="WrapWithOverflow"></TextBlock>
                        <Button Grid.Row="4" Content="Word" Name="BtnWord" Click="BtnWord_Click" Width="50px" Background="DeepSkyBlue"></Button>
                        
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
